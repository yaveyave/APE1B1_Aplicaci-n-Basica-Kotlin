<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/ediText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="text"
        android:textAlignment="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.374" />

    <Button
        android:id="@+id/btn_mostrar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="88dp"
        android:text="Mostrar"
        app:layout_constraintEnd_toEndOf="@+id/ediText"
        app:layout_constraintStart_toStartOf="@+id/ediText"
        app:layout_constraintTop_toBottomOf="@+id/ediText" />

    <Button
        android:id="@+id/btn_siguiente"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="213dp"
        android:padding="15dp"
        android:text="Siguiente"
        app:layout_constraintEnd_toEndOf="@+id/ediText"
        app:layout_constraintStart_toStartOf="@+id/ediText"
        app:layout_constraintTop_toTopOf="@+id/ediText" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="264dp"
        android:text="Powered by Oscar Y"
        android:textColor="#CC87EF"
        app:layout_constraintBottom_toBottomOf="@+id/ediText"
        app:layout_constraintEnd_toEndOf="@+id/ediText"
        app:layout_constraintStart_toStartOf="@+id/ediText" />

</androidx.constraintlayout.widget.ConstraintLayout>